pipeline {
    agent any
    
    environment {
        GEMINI_API_KEY = credentials('GEMINI_API_KEY')
    }
    
    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main', 
                    url: 'https://github.com/Ayzazkhan/Godady-digilaxy-partner-page-automation.git',
                    credentialsId: 'github-credentials'
            }
        }
        
        stage('Setup Python Environment') {
            steps {
                dir('content-generation') {
                    sh '''
                    python3 -m venv venv
                    . venv/bin/activate
                    pip install --upgrade pip
                    pip install google-generativeai requests
                    '''
                }
            }
        }
        
        stage('Generate Content') {
            steps {
                dir('content-generation') {
                    sh '''
                    . venv/bin/activate
                    python3 content_generator.py
                    '''
                }
            }
        }
        
        stage('Verify Content') {
            steps {
                dir('content-generation') {
                    sh '''
                    echo "🔍 DEEP VERIFICATION:"
                    echo "📁 Current files:"
                    ls -la
                    echo "📄 File details:"
                    ls -la generated_content.json
                    echo "📊 File content (first 2000 chars):"
                    head -c 2000 generated_content.json
                    echo ""
                    echo "📈 Detailed validation:"
                    python3 -c "
import json
import os
try:
    with open('generated_content.json', 'r', encoding='utf-8') as f:
        data = json.load(f)
    print(f'✅ SUCCESS: {len(data)} content pieces generated')
    print(f'📝 Total links: {sum(item.get(\\\"links_count\\\", 0) for item in data)}')
    print(f'📝 Sample content:')
    for i in range(min(2, len(data))):
        print(f'  {i+1}. {data[i][\\\"keyword\\\"]} - Links: {data[i].get(\\\"links_count\\\", 0)}')
        print(f'     Preview: {data[i][\\\"content\\\"][:100]}...')
except Exception as e:
    print(f'❌ ERROR: {e}')
    print('Trying alternative encoding...')
    try:
        with open('generated_content.json', 'r') as f:
            content = f.read()
        print(f'File content: {content[:500]}')
    except Exception as e2:
        print(f'File read error: {e2}')
"
                    '''
                }
            }
        }
        
        stage('Push to GitHub') {
            steps {
                script {
                    withCredentials([usernamePassword(credentialsId: 'github-credentials', 
                    usernameVariable: 'GIT_USERNAME', passwordVariable: 'GIT_PASSWORD')]) {
                        dir('content-generation') {
                            sh '''
                            echo "🚀 Pushing to GitHub..."
                            
                            # Git configuration
                            git config --global user.email "ayzazkhna82@gmail.com"
                            git config --global user.name "Ayzazkhan"
                            
                            # Check git status
                            git status
                            
                            # Add the generated file
                            git add generated_content.json
                            
                            # Check if there are changes
                            if git diff --cached --quiet; then
                                echo "⚠️ No changes to commit"
                            else
                                # Commit changes
                                git commit -m "Auto-generated: 175 content pieces with links [Jenkins Build ${BUILD_NUMBER}]"
                                
                                # Push to GitHub using credentials
                                git push https://${GIT_USERNAME}:${GIT_PASSWORD}@github.com/Ayzazkhan/Godady-digilaxy-partner-page-automation.git main
                                echo "✅ Successfully pushed to GitHub!"
                            fi
                            '''
                        }
                    }
                }
            }
        }
    }
    
    post {
        always {
            sh 'rm -rf content-generation/venv'
            archiveArtifacts artifacts: 'content-generation/generated_content.json', fingerprint: true
        }
        success {
            echo '🎉 Content generation completed successfully!'
        }
    }
}
